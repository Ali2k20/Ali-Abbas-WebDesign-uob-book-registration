<!DOCTYPE html>
<html lang="en">
<head>
    <?php
    session_start();
    require("conn.php");
    if ($_SESSION['Type'] === "Admin") {
        header("Location: Admin.php");
        exit();
    }
    
    // Redirect unauthorized users to the Sign-In page
    if ($_SESSION['Type'] !== "student" && $_SESSION['Type'] !== "staff") {
        header("Location: signin.php");
        exit();
    }

    $id = $_SESSION["userid"];
    $query1 = $db->prepare("SELECT * FROM user where id =? ");
    $query1->bindParam(1, $id);
    $query1->execute();
    $s = $query1->fetch()
    ?>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            margin: 0;
            background-color: ;
        }
        
        nav {
            height: 150px;
            background-color: #004fa3;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .image-container {
            display: flex;
            align-items: center;
            margin: 0;
            background-color: white;
            height: 130px;
            width: 105px;
            margin-left: 30px;
            border-bottom-left-radius: 40px;
            border-bottom-right-radius: 40px;
            padding: 12px;
        }

        img {
            height: 120px;
            width: auto;
        }

        .SearchBar {
            background-color: purple;
            height: 90%;
            width: 20%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .SearchBar form {
            display: flex;
            align-items: center;
            width: 100%;
        }

        .SearchBar input[type="search"] {
            padding: 10px;
            border: none;
            border-radius: 20px 0 0 20px;
            width: calc(100% - 80px); /* Adjusts the width of search input */
        }

        .SearchBar input[type="submit"] {
            padding: 10px 20px;
            border: none;
            border-radius: 0 20px 20px 0;
            background-color: #555;
            color: white;
            cursor: pointer;
            width: 80px; /* Adjusts the width of submit button */
        }

        .SearchBar input[type="submit"]:hover {
            background-color: #777;
        }

        .Menu ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 20px;
            margin-right: 20px;
        }

        .Menu ul li {
            margin: 0;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

        .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }

        .dropdown-content a:hover {
            background-color: #f1f1f1;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropbtn {
            background: none;
            border: none;
            padding: 0;
            cursor: pointer;
        }

        .dropbtn img {
            border-radius: 50%;
            width: 80px;
            margin-right: 15px;
            height: auto;
        }
       
    </style>
</head>
<body>
    <nav>
        <div class="image-container">
            <img src="images/logo.png" alt="Logo">
        </div>
        <div class="SearchBar">
            <form action="">
                <input type="search" placeholder="Search...">
                <input type="submit" value="Search">
            </form>
        </div>
        <div class="Menu">
            <ul>
            
            <li><a href="">Your Current Booking</a></li>
                <li><a href="">Your Current Booking</a></li>
                <li>
                    <div class="dropdown">
                        <button class="dropbtn">
                            <img class="profile-picture-update" src="images/<?php if($s){ echo $s['pfp']; } ?>" alt="Profile Picture">
                        </button>
                        <div class="dropdown-content">
                        <a href="HomePage.php">Home</a>
                            <a href="view_profile.php">View Profile</a>
                            <a href="profile.php">Edit Profile</a>
                            <a href="logout.php">Log Out</a>
                        </div>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
    <div class="Rooms">
    <div class="Room-container">
        <h1>Welcome to UOB Booking Room Site 
            <?php if($_SESSION['Type']=="student")  echo "Student {$s['FullName']}" ; else
            echo "Instructor {$s['FullName']}"; ?>
        </H1>
    </div>
</div>
   
</body>
</html>
